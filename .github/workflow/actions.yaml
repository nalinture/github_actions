name: first_workflow

on: 
  push:
    branches:
      - main
     
     
jobs:
   hello:
    runs_on: ubuntu_latest
    steps:lig
      - name: vanakkam
        run: echo "vanakkam"
        shell: bash
